# 產品需求：自由地圖收藏、共編與分享平台

## 一、產品概念

本產品是一個高度自由的地圖收藏網站，使用者可以依照自己的需求建立不同主題分類，並在地圖上紀錄、編輯、查看與分享各種地點或體驗。

它不只限於美食，也可以用來整理：

* 想去的地方
* 想玩的景點
* 想看的展覽
* 已經去過的店家
* 一定要去的清單
* 朋友推薦清單
* 旅遊行程清單
* 約會地點清單
* 個人踩點地圖

使用者可以自由建立分類，例如拉麵、螺螄粉、咖啡廳、甜點店、酒吧、展覽、旅遊景點、濟州島想去清單、台北約會地圖等。

網站核心目標是讓使用者可以透過直覺、美觀、好操作的 GUI，在地圖上快速建立自己的地點資料庫，並透過 Google Maps 高度整合，方便跳轉導航、查找資訊與延伸探索相關地點。

---

## 二、產品核心價值

本產品不是單純的地圖標記工具，而是一個可以自由分類、多人共編、即時同步、產出分享內容的地圖收藏平台。

核心價值包含：

* 使用者可以自由建立任何主題分類
* 每個地點都能被記錄、標註、評價與管理
* 可以單獨查看某一個分類，例如只看拉麵、咖啡廳或景點
* 可以依照目前位置快速尋找附近已收藏或已定義過的地點
* 與 Google Maps 深度整合，方便搜尋、跳轉與導航
* 支援帳號、多人共編與權限管理
* 共編者可以即時看到彼此的新增、編輯與刪除操作
* 可以將地圖或清單產出成圖片，方便社群分享
* 可根據使用者收藏與評價進行智慧推薦

---

## 三、核心功能

## 1. 帳號系統

由於產品包含個人收藏、共編地圖、權限管理與分享功能，因此需要帳號系統。

帳號功能包含：

* 使用者註冊
* 使用者登入
* 使用者登出
* 個人資料管理
* 我的地圖清單
* 我的分類清單
* 我參與共編的地圖
* 權限驗證
* 後續可支援第三方登入，例如 Google Login

---

## 2. 自由分類管理

使用者可以自行新增、編輯、刪除分類，不限制分類類型。

分類可以是美食，也可以是景點、活動、展覽、旅遊、購物或任何想收藏的主題。

分類範例：

* 拉麵
* 螺螄粉
* 咖啡廳
* 甜點店
* 酒吧
* 火鍋
* 想去景點
* 已去景點
* 想玩的地方
* 想看的展覽
* 約會清單
* 一定要去
* 個人踩點地圖
* 濟州島行程
* 東京美食地圖

每個分類可設定：

* 分類名稱
* 分類描述
* 分類顏色
* 分類圖示
* 是否公開
* 是否可共編
* 排序順序
* 建立者
* 建立時間
* 更新時間

---

## 3. 單一分類查看與附近搜尋

使用者可以單獨挑出某一個分類進行查看，例如只查看「拉麵」分類。

當使用者切換到某個分類時，地圖與清單只會顯示該分類底下的地點，方便快速找到目前附近已經收藏或定義過的選項。

使用情境範例：

* 使用者今天想吃拉麵，可以切換到「拉麵」分類。
* 系統只顯示使用者已收藏、想去、已去或標記過的拉麵店。
* 使用者可以依照目前位置快速查看附近有哪些拉麵店。
* 可以篩選「想去」、「已去」、「一定要去」、「高評價」等狀態。
* 可以直接點擊地點，跳轉到 Google Maps 查看路線與導航。

此功能重點是讓使用者不只是收藏地點，而是可以在需要時，快速從自己的資料庫中找到附近可去的地方。

例如：

> 今天突然想吃拉麵，使用者只要打開「拉麵」分類，就能看到附近有哪些自己曾經收藏、朋友推薦、已經吃過或還沒吃過的拉麵店。

---

## 4. 地點紀錄與地圖標註

使用者可以在地圖上新增地點，並為每個地點建立詳細紀錄。

每個地點可包含：

* 地點名稱
* 地址
* 經度
* 緯度
* Google Maps 連結
* Google Place ID
* 所屬地圖
* 所屬分類
* 標籤
* 狀態
* 評價
* 備註
* 照片
* 想去原因
* 實際體驗紀錄
* 推薦程度
* 建立者
* 最後更新者
* 建立時間
* 最後更新時間

狀態可以自由設定，例如：

* 想去
* 已經去過
* 一定要去
* 普通
* 不推薦
* 拉完了
* 收藏中
* 待確認
* 已完成
* 想再訪
* 朋友推薦

---

## 5. Google Maps 高度整合

當使用者在網站上標記或新增地點時，系統需要自動產生或綁定 Google Maps 連結，讓使用者可以快速跳轉到 Google Maps 查看位置、導航與詳細資訊。

Google Maps 整合需求包含：

* 自動搜尋地點並取得地址
* 自動取得經緯度
* 自動產生 Google Maps 跳轉連結
* 支援一鍵開啟 Google Maps
* 支援 Google Maps 導航
* 顯示地點基本資訊
* 儲存 Google Place ID
* 支援依目前位置查看附近收藏地點
* 避免使用者需要手動複製名稱再切換到 Google Maps 搜尋
* 可依照使用者標記的地點，推薦附近或類似地點

使用情境範例：

* 使用者新增一間拉麵店。
* 系統自動搜尋該店家的 Google Maps 資訊。
* 系統儲存地址、經緯度、Google Maps 連結與 Place ID。
* 使用者之後點擊該地點，就可以直接開啟 Google Maps 導航。
* 當使用者切換到「拉麵」分類時，可以快速查看附近有哪些已收藏的拉麵店。

---

## 6. 主題切換與地圖視圖

使用者可以快速切換不同主題地圖或分類。

例如：

* 從「拉麵地圖」切換到「螺螄粉地圖」
* 從「美食地圖」切換到「旅遊景點地圖」
* 從「想去清單」切換到「已去清單」
* 從「個人地圖」切換到「朋友共編地圖」
* 從「台北地圖」切換到「濟州島地圖」

切換後，地圖上只顯示該分類或篩選條件下的地點。

地圖視圖需求包含：

* 地圖模式
* 清單模式
* 地圖＋清單混合模式
* 地點詳情側邊欄
* 分類篩選
* 狀態篩選
* 評價篩選
* 距離排序
* 附近地點排序
* 搜尋地點

---

## 7. 評價與標籤系統

使用者可以針對地點給予評價與標籤。

評價方式可包含：

* 星等評分
* 文字心得
* 推薦程度
* 自訂評價標籤
* 是否想再訪
* 是否推薦朋友

標籤範例：

* 一定要去
* CP 值高
* 排隊很久
* 適合約會
* 適合拍照
* 交通方便
* 想再訪
* 不會再去
* 朋友推薦
* IG 收藏
* 便宜
* 氣氛好
* 服務好
* 份量大
* 適合多人

---

## 8. 共編協作

使用者可以邀請朋友一起編輯同一張地圖或同一個分類清單。

共編功能包含：

* 邀請他人加入地圖
* 設定權限
* 顯示每個地點由誰新增
* 顯示更新紀錄
* 多人共同維護清單
* 移除共編者
* 修改共編者權限
* 顯示共編成員清單

權限可分為：

* 擁有者 Owner：可管理整張地圖、分類、地點與成員
* 編輯者 Editor：可新增、編輯、刪除地點
* 檢視者 Viewer：只能查看，不能編輯

使用情境範例：

* 朋友一起整理東京美食地圖
* 情侶共同建立約會地點清單
* 旅伴一起規劃濟州島想去景點
* 社群一起整理拉麵店地圖
* 同事一起整理出差可去餐廳

---

## 9. 即時同步與共編推播

由於本產品包含帳號系統、共編地圖、多人協作與地點編輯功能，因此需要支援即時同步機制，讓不同使用者在同一張地圖或同一個分類中操作時，可以即時看到彼此的變更。

如果沒有即時同步，使用者 A 編輯地點後，使用者 B 可能需要重新整理頁面才會看到變更。為了讓共編體驗更順暢，需要加入 WebSocket 即時同步機制。

### 9.1 即時同步需求

當使用者對地圖資料進行新增、編輯或刪除時，其他正在查看同一張地圖的共編者，應該可以即時看到更新結果，不需要手動重新整理頁面。

需要即時同步的操作包含：

* 新增地點
* 編輯地點名稱
* 修改地址
* 修改分類
* 修改狀態
* 修改評價
* 新增或刪除標籤
* 新增備註
* 上傳或更新照片
* 刪除地點
* 新增共編者
* 移除共編者
* 修改共編權限
* 新增分類
* 修改分類
* 刪除分類

### 9.2 共編推播情境

使用情境範例：

* A 使用者新增一間拉麵店，B 使用者的地圖上會即時出現新的標記。
* A 使用者把某間店狀態改成「已去」，B 使用者的清單也會即時更新。
* A 使用者修改評價或備註，B 使用者可以在同一張地圖中看到最新內容。
* 多人一起規劃旅行清單時，每個人新增的景點會即時同步到所有共編者畫面。
* A 使用者刪除某個地點，其他共編者畫面上的 marker 與清單項目會同步移除。

### 9.3 技術建議

後端可使用 Django Channels 建立 WebSocket 連線，讓前端 React 可以即時接收地圖資料變更事件。

建議技術組合：

* React + Vite：前端畫面與 WebSocket client
* Django REST Framework：一般資料 CRUD API
* Django Channels：處理 WebSocket 即時同步
* Redis：作為 Channels layer，負責 WebSocket 訊息分發
* PostgreSQL：儲存正式資料
* Docker：管理 frontend、backend、postgresql、redis 等服務
* Nginx：正式環境反向代理 HTTP 與 WebSocket

### 9.4 即時事件設計

系統可設計以下事件類型：

* place.created
* place.updated
* place.deleted
* category.created
* category.updated
* category.deleted
* review.created
* review.updated
* review.deleted
* tag.created
* tag.updated
* tag.deleted
* collaborator.added
* collaborator.removed
* permission.updated
* map.updated

每當某位使用者完成資料異動後，後端會將事件推送給正在查看同一張地圖的其他共編者。

### 9.5 前端畫面更新方式

前端接收到 WebSocket 事件後，需即時更新畫面狀態。

例如：

* 新增地點時，地圖立即新增 marker
* 編輯地點時，右側資訊卡立即更新
* 刪除地點時，地圖 marker 與清單項目同步移除
* 修改狀態時，分類篩選與標籤顯示同步更新
* 新增共編者時，成員清單即時更新
* 修改權限時，前端立即更新可操作範圍

### 9.6 即時同步與資料一致性

雖然畫面需要即時更新，但正式資料仍以 PostgreSQL 為主。

建議流程：

1. 使用者在前端送出新增、編輯或刪除操作。
2. 前端呼叫 Django REST API。
3. 後端驗證使用者權限。
4. 後端寫入 PostgreSQL。
5. 寫入成功後，後端透過 WebSocket 推播事件。
6. 其他共編者收到事件後更新畫面。

這樣可以確保資料不是只在前端即時變更，而是有正式寫入資料庫後才同步給其他人。

---

## 10. 圖片產出與分享

使用者可以將自己的地圖、分類清單或地點紀錄產出成圖片，方便分享到社群平台。

圖片分享需求包含：

* 產出地圖截圖
* 產出分類清單圖片
* 產出推薦地點卡片
* 產出排行榜圖片
* 支援分享到 IG、Threads、LINE、Facebook 等平台
* 圖片版面需美觀，適合社群分享
* 可選擇是否顯示地點名稱、評價、標籤、備註
* 可產生公開分享連結

使用情境範例：

* 使用者整理完「台北必吃拉麵地圖」後，可以產出漂亮圖片分享到 IG。
* 使用者與朋友共編完「濟州島想去清單」後，可以產出行程地圖圖片分享到 LINE 群組。
* 使用者可以產出「我吃過的 10 間拉麵」排行榜圖片。

---

## 11. 智慧推薦

系統可以根據使用者已收藏、已標記、已評價的地點，自動推薦類似或相關內容。

推薦邏輯可包含：

* 同類型地點推薦
* 附近地點推薦
* 高評價地點推薦
* 與目前收藏相似的地點推薦
* 朋友也收藏的地點
* 同一區域可順路去的地點
* 依目前分類推薦相關地點

例如：

* 使用者收藏多間拉麵店，系統推薦附近其他高評價拉麵店。
* 使用者標記濟州島想去景點，系統推薦附近咖啡廳、餐廳或拍照景點。
* 使用者建立螺螄粉清單，系統推薦相關中式小吃或相似店家。
* 使用者目前查看「咖啡廳」分類，系統推薦附近其他適合收藏的咖啡廳。

---

# 四、主要使用情境

## 情境一：建立拉麵地圖

使用者建立一個「拉麵」分類，新增多間拉麵店，並標記想去、已去、一定要去等狀態。每間店都自動附上 Google Maps 連結，方便之後直接導航。

## 情境二：快速查看附近拉麵

使用者今天想吃拉麵，可以單獨切換到「拉麵」分類。系統會只顯示拉麵相關地點，並可依目前位置排序附近已收藏或已標記的拉麵店，讓使用者快速決定要去哪一家。

## 情境三：切換到螺螄粉地圖

使用者新增另一個「螺螄粉」分類，切換後地圖只顯示螺螄粉相關地點。每個地點可以記錄口味、評價、是否再訪等資訊。

## 情境四：建立旅遊清單

使用者建立「濟州島想去景點」分類，把想玩、想看、想吃的地方全部標在地圖上，並邀請旅伴一起共編。

## 情境五：多人共編即時同步

A 使用者新增一個景點，B 使用者正在同一張地圖上查看時，畫面會即時出現新地點，不需要重新整理。

## 情境六：產出分享圖片

使用者整理完一份「台北必吃拉麵地圖」後，可以產出漂亮的圖片，分享到 IG 或 LINE 群組。

## 情境七：智慧推薦附近地點

使用者收藏幾間店後，系統根據地點類型與區域，自動推薦附近類似店家或可以順路安排的地點。

---

# 五、產品特色

* 不限美食，任何地點與體驗都可以收藏
* 分類完全自由，可由使用者自行新增
* 可單獨查看某個分類，例如只看拉麵、咖啡廳或景點
* 支援依目前位置快速尋找附近已收藏地點
* 地圖視覺化操作，GUI 直覺好用
* 可標註狀態、評分、心得與標籤
* 與 Google Maps 高度整合，方便查找、跳轉與導航
* 支援帳號系統與個人收藏
* 支援多人共編，適合朋友、情侶、旅伴或社群一起使用
* 支援 WebSocket 即時同步，共編者可即時看到變更
* 可產出美觀圖片，方便社群分享
* 支援智慧推薦，幫助使用者探索更多類似地點

---

# 六、指定技術架構

## 1. Frontend

使用 React + Vite 作為前端框架。

前端主要負責：

* 地圖操作介面
* 分類切換
* 地點清單顯示
* 地點詳細資料頁
* 標籤與狀態篩選
* 共編介面
* 圖片分享畫面
* Google Maps 操作與跳轉
* WebSocket 即時事件接收
* RWD 響應式設計

指定技術：

* React
* Vite
* TypeScript，建議使用
* React Router
* 狀態管理可使用 Zustand 或 Redux Toolkit
* UI Library 可使用 Mantine、MUI 或 Tailwind CSS
* Google Maps JavaScript API
* WebSocket client

---

## 2. Backend

使用 Django 作為後端框架，並使用 uv 進行 Python 套件管理與虛擬環境管理。

後端主要負責：

* 使用者帳號管理
* 身分驗證與權限管理
* 地圖管理
* 分類管理
* 地點資料 CRUD
* 標籤與狀態管理
* 評價與心得管理
* 共編權限管理
* Google Maps 資料儲存與查詢
* 智慧推薦 API
* 圖片分享資料生成
* WebSocket 即時同步
* API 權限驗證

指定技術：

* Django
* Django REST Framework
* Django Channels
* uv
* Python
* PostgreSQL
* Redis
* Docker

---

## 3. Database

使用 PostgreSQL 作為主要資料庫。

主要資料表可包含：

* users
* maps
* categories
* places
* place_tags
* tags
* place_statuses
* reviews
* collaborators
* permissions
* share_images
* recommendation_logs
* activity_logs

PostgreSQL 需支援：

* 地點資料儲存
* 分類關聯
* 權限關聯
* 評價與標籤查詢
* 依座標查詢附近地點
* 地圖共編關聯
* 歷史紀錄查詢

後續可評估加入 PostGIS 支援地理空間查詢，讓附近搜尋、距離排序與地理範圍查詢更精準。

---

## 4. Realtime

即時同步使用 WebSocket。

建議技術：

* Django Channels
* Redis Channel Layer
* React WebSocket Client
* ASGI Server，例如 Daphne 或 Uvicorn

即時同步用途：

* 多人共編地圖
* 地點新增即時顯示
* 地點修改即時更新
* 地點刪除即時移除
* 成員權限異動即時更新
* 分類異動即時更新

---

## 5. Docker

整個專案需支援 Docker 化部署。

建議包含：

* frontend container
* backend container
* PostgreSQL container
* Redis container
* Nginx container，正式環境建議
* docker-compose 設定
* 環境變數管理
* volume 管理資料庫資料
* migration 執行流程

---

# 七、環境規劃

本專案需分為三個環境：

* Development 開發環境
* Staging 驗證環境
* Production 正式環境

---

## 1. Development 開發環境

用途：本機開發與功能測試。

特性：

* 使用本機 Docker Compose 啟動
* 前端支援 hot reload
* 後端支援 debug mode
* 使用本機 PostgreSQL container
* 使用本機 Redis container
* WebSocket 可連接本機 backend
* 可使用測試 Google Maps API Key
* 適合快速開發與調整 UI

環境名稱建議：

```text
development
```

---

## 2. Staging 驗證環境

用途：功能驗證、整合測試、正式上線前檢查。

特性：

* 模擬正式環境設定
* 使用獨立 PostgreSQL database
* 使用獨立 Redis
* 使用獨立環境變數
* 可供測試人員或朋友試用
* 用來確認 Google Maps 整合、共編、權限、圖片分享功能是否正常
* 測試多人共編、WebSocket 即時更新與斷線重連
* 部署流程需盡量接近正式環境

環境名稱建議：

```text
staging
```

---

## 3. Production 正式環境

用途：正式提供使用者使用。

特性：

* 關閉 Django Debug
* 使用正式 PostgreSQL database
* 使用正式 Redis
* 使用正式 Google Maps API Key
* 啟用 HTTPS
* 使用 Nginx 或雲端服務做反向代理
* Nginx 需支援 WebSocket proxy
* 需設定資料庫備份機制
* 需設定錯誤監控與日誌紀錄
* 前端需 build 成 production 靜態檔
* 後端需使用正式 ASGI server，例如 Daphne 或 Uvicorn
* 需處理 WebSocket 權限驗證與斷線重連

環境名稱建議：

```text
production
```

---

# 八、建議系統架構

```text
React + Vite Frontend
        |
        | REST API
        v
Django REST Framework Backend
        |
        | Read / Write
        v
PostgreSQL Database

React + Vite Frontend
        |
        | WebSocket
        v
Django Channels
        |
        v
Redis Channel Layer
        |
        v
Broadcast realtime updates to collaborators
```

正式環境可加入：

```text
User
 |
 v
Nginx
 |
 |-- Frontend Static Files
 |
 |-- Django API
 |
 |-- WebSocket Proxy
```

---

# 九、API 與同步設計概念

## 1. REST API

一般資料操作使用 REST API。

例如：

* 建立地圖
* 編輯分類
* 新增地點
* 修改地點資料
* 刪除地點
* 新增評價
* 邀請共編者
* 修改權限

## 2. WebSocket

即時同步使用 WebSocket。

例如：

* 地點新增後推播給同一張地圖的其他使用者
* 地點更新後推播更新事件
* 地點刪除後推播刪除事件
* 權限變更後推播權限更新事件

## 3. 推薦流程

智慧推薦可先從簡單規則開始，未來再擴充 AI 或演算法。

第一階段可先支援：

* 同分類推薦
* 附近地點推薦
* 高評價推薦
* 使用者常收藏類型推薦

---

# 十、MVP 第一階段功能建議

第一階段可以先做出最小可用版本，避免一開始功能太大。

MVP 建議包含：

* 使用者登入 / 註冊
* 建立地圖
* 建立分類
* 新增地點
* 編輯地點
* 刪除地點
* 地圖上顯示 marker
* 地點清單顯示
* 單一分類切換查看
* 狀態標註，例如想去、已去、一定要去
* Google Maps 連結跳轉
* PostgreSQL 儲存資料
* Docker 開發環境

---

# 十一、第二階段功能建議

第二階段加入共編與即時同步。

功能包含：

* 邀請共編者
* 權限管理
* Django Channels
* Redis
* WebSocket 即時同步
* 地點新增即時顯示
* 地點修改即時更新
* 地點刪除即時移除
* 共編成員清單
* 操作紀錄

---

# 十二、第三階段功能建議

第三階段加入分享與推薦。

功能包含：

* 地圖圖片產出
* 地點卡片產出
* 分享連結
* 社群分享
* 附近推薦
* 同分類推薦
* 高評價推薦
* 朋友收藏推薦
* PostGIS 地理空間查詢優化

---

# 十三、GitHub Repository Description

A flexible map-based collection app with Google Maps integration, custom categories, nearby place discovery, realtime collaboration, ratings, visual sharing, and smart recommendations.

---

# 十四、產品一句話說明

一個可以自由建立分類、在地圖上收藏與管理地點，並結合 Google Maps、附近搜尋、共編即時同步、評價、圖片分享與智慧推薦的地圖收藏平台。

---

# 十五、技術一句話說明

本專案使用 React + Vite 建立前端，Django + Django REST Framework + Django Channels 建立後端，uv 管理 Python 環境，PostgreSQL 儲存資料，Redis 支援 WebSocket 即時同步，並透過 Docker 管理開發、驗證與正式三個環境。

